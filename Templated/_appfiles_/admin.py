# Django-Autogenerator

from .models import *
from _libraries_admin import *

# Uncomment the class below and replace `ModelName` with your own model.
# class ModelNameAdmin(admin.ModelAdmin):
#     ordering = ['-name']
#     #list_display = ['name', 'slug']
#     #list_editable = ['name']


# Register your model to the admin site.
#admin.site.register(ModelName, ModelNameAdmin)
